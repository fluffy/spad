
sudo: required

language: go

services:
  - docker

before_install:
  - docker build -t spad .

script:
  - docker run -it -v `pwd`:/work spad

deploy:
    provider: script
    script: deploy.sh
    on:
        branch: master
